services:
  backend:
    build: ./backend
    expose: ["8000"]
    volumes: ["./backend/data:/app/data", "./frontend/public/geo:/app/geo:ro"]
    environment:
      DATABASE_URL: "sqlite:///./data/crimemap.db"
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/api/stats')"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks: [default, coolify]

  frontend:
    build: ./frontend
    expose: ["3000"]
    depends_on: [backend]
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks: [default, coolify]
    labels:
      - traefik.enable=true
      - traefik.http.routers.crimemap-http.rule=Host(`crimebrasil.com.br`)
      - traefik.http.routers.crimemap-http.entrypoints=http
      - traefik.http.routers.crimemap-http.middlewares=redirect-to-https
      - traefik.http.routers.crimemap-https.rule=Host(`crimebrasil.com.br`)
      - traefik.http.routers.crimemap-https.entrypoints=https
      - traefik.http.routers.crimemap-https.tls=true
      - traefik.http.routers.crimemap-https.tls.certresolver=letsencrypt
      - traefik.http.services.crimemap.loadbalancer.server.port=3000
      - traefik.http.middlewares.redirect-to-https.redirectscheme.scheme=https

networks:
  coolify:
    external: true
